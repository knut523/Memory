// Placeholder for the proprietary cost-guard submodule (absent in this
// open-source checkout -- see MemoryProxy/src/storage/factory.ts).
// Only reached when storage backend is "cos"; this deployment doesn't use it.
export {};
